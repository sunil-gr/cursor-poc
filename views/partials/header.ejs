<header class="main-header">
  <div class="header-content">
    <img src="/public/logo.png" alt="Logo" class="header-logo">
    <span class="header-title">Cursor Interactive Metrics</span>
    <% if (typeof session !== 'undefined' && session.authenticated) { %>
      <a href="/logout" class="header-logout">Logout</a>
    <% } %>
  </div>
</header> 